$goal
你正在偏离 Stage 8 范围。请停止扩展功能并纠偏。

Stage 8 只允许做：
- local-first session storage；
- export/import；
- hide/delete/clear controls；
- redaction；
- privacy notice；
- tests。

请移除或停止以下越界内容：
- login/auth/user account；
- payment；
- database；
- cloud sync；
- analytics tracking；
- new AI provider；
- real .env；
- real API key；
- ranking changes；
- rectification changes；
- forecast semantic changes；
- React/Next/Vite/Vue/Svelte migration。

然后运行 npm test，并汇报是否恢复到 Stage 8 范围。
