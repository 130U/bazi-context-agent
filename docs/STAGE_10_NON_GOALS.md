# Stage 10 Non-Goals

Stage 10 must not:

- change prediction logic;
- change BaZi adapter logic;
- change rectification scoring;
- change evaluation logic;
- add a backend database;
- add login/payment/user system;
- add real OpenAI calls;
- add real `.env`;
- expose API keys;
- change repository visibility to public automatically;
- choose a license automatically;
- claim guaranteed accuracy.
